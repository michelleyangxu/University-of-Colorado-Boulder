# 决策问题、语言与复杂度类 — 学习笔记

> 本讲为**计算理论 (Theory of Computation)** 的入门,主题:决策问题、语言、图灵机、可判定性。
> 为下一讲 **P 与 NP** 打基础。

---

## 一、核心动机:研究"问题本身的难度"

计算理论研究的不是某个具体算法,而是**问题本身固有的难度 (inherent difficulty)**。

按难度可把所有问题分成三类:

1. **本质容易** — 现代计算机可以快速解决。
2. **本质困难** — 我们已证明它们必然慢(甚至不可解)。
3. **未知** — 不知道是问题本身困难,还是人类还没想出聪明的办法。

---

## 二、什么是"决策问题" (Decision Problem)

### 1. 定义

把算法看作一个"黑箱":输入进去 → 输出**只有 Yes 或 No**。
这样的问题称为**决策问题**。

```
   输入 ──►  ┌──────────────┐  ──► Yes / No
            │   黑箱算法    │
            └──────────────┘
```

### 2. 决策问题的例子

| 问题 | 输入 | 输出 |
|---|---|---|
| **BelongsToArray** | 数组 A,元素 e | e 是否在 A 中? |
| **PathExists** | 图 G,顶点 s, t | s 到 t 是否连通? |
| **IsPrime** | 二进制表示的整数 n | n 是否为素数? |

### 3. 不是决策问题的例子

- **排序** — 输出是有序数组,不是 Yes/No。
- **0-1 背包 (Knapsack)** — 输出是选中的物品集合。
- **因数分解 (Factoring)** — 输出是一个最小因子 p。

---

## 三、把"一般问题"转成"决策版本"

很多非决策问题都可以**改写**成一个等价的决策问题,而且**用这个决策版本作为子程序,就能解出原问题**。

### 例 1:因数分解 → Factor?

- 原问题:给 n,求最小因子 p。
- 决策版本:给 n 和位索引 i,问 **p 的第 i 位是不是 1?**
- 解法:从 i = 0 到 log n / 2,反复调用决策版本,即可逐位拼出 p。
- 关键点:最小因子的位数 ≤ n 的位数的一半 ⇒ 只需约 ½·log n 次调用。

### 例 2:背包 → Knapsack?

- 原问题:在重量 ≤ W 下,选出**价值最大**的物品子集。
- 决策版本:**是否存在**总重 ≤ W 且总价值 ≥ V 的子集?
- 解法:对 V 做**二分搜索**,即可逼近最大价值。

### 例 3:排序(反例)

只能问"e 是不是排好序后的第 i 个元素?"——比较牵强。
**结论:并不是所有问题都有"漂亮"的决策版本**,但我们关心的大多数问题都有。

---

## 四、为什么决策问题更好研究?

1. **答案大小固定为 1 bit** — 比较不同问题的复杂度时,不用考虑"输出有多大"。
   - 反例:"算 π 的前 10000 位"——答案本身就很大,讨论复杂度意义不大。
2. **可以和"语言"建立等价关系**(下一节)。

---

## 五、问题 ↔ 语言 (Language)

### 1. 定义

对一个决策问题,**所有让答案为 Yes 的输入** 组成的集合,叫做该问题的**语言** L。

```
L = { 所有 Yes-输入的编码字符串 }
```

### 2. 例子

- **L_prime** = {10, 11, 101, 111, 1011, ...} (素数 2, 3, 5, 7, 11 ... 的二进制)
- **L_path** = { ⟨G, s, t⟩ | G 中 s 与 t 连通 }

所有输入都编码成 **0/1 二进制串**。语言就是 {0,1}* 的一个子集。

### 3. 算法 ↔ 识别语言

> "一个算法" = "一个识别语言 L 的盒子",输入 x,判断 x ∈ L?

---

## 六、计算的统一模型:图灵机

### 1. 图灵机 (Turing Machine)

由 Alan Turing 提出 —— 现代计算机科学之父。
极简模型:

- 一条**纸带 (tape)**,上面写着输入字符串。
- 一个**读写头**,可以读字符、左移或右移。
- 一个**有限状态控制器**,根据当前状态 + 读到的字符,决定:
  - 写什么、往哪移、转到哪个新状态。
- 在某个状态停机,输出 **Accept (Yes) / Reject (No)**。

转移规则形如:
```
(S1, 读到 0) → (S2, 右移)
(S2, 读到 1) → (S2, 右移)
(S2, 读到 0) → (S_accept)
```

### 2. Church-Turing 论题

**任何物理上可实现的计算装置,其计算能力都等价于图灵机。**

这是一个**信念/假说,不是定理**,但已有大量证据支持:

| 计算模型 | 提出者 | 与图灵机的关系 |
|---|---|---|
| RAM 机(我们写伪代码的模型) | — | 等价 |
| λ-演算 | Alonzo Church | 等价 |
| Post 机 | Emil Post | 等价 |
| Minsky 机 | Marvin Minsky | 等价 |
| 量子计算机 | — | 等价(在"可计算"层面) |

⇒ 我们写在 RAM 模型上的伪代码,本质上都能由图灵机实现。所以平时无需操心"算法物理上是否可实现"。

---

## 七、第一道分界线:可判定 vs 不可判定

```
       全部问题
   ┌──────────────────────────────┐
   │   ┌────────────────────┐     │
   │   │  可判定 Decidable  │     │
   │   │  (存在算法)         │     │
   │   └────────────────────┘     │
   │                              │
   │   不可判定 Undecidable        │
   │   (任何图灵机都解不了)       │
   └──────────────────────────────┘
```

### 1. 可判定问题 (Decidable)

存在图灵机能在有限步内停机并给出正确答案。
本课中遇到的几乎所有问题都在这里:DFS 找环、素性判断、Factor?、Knapsack? 等。

### 2. 不可判定问题 (Undecidable)

**没有任何算法**能解决。这是一个惊人的结论:**不是所有我们关心的问题都有算法**。

#### 例 1:停机问题 (Halting Problem)

> 给定一个程序(比如 `dfs.py`),判断它是否会进入死循环。

Turing 证明:**不存在**这样的"智能编译器"。

#### 例 2:丢番图方程 (Diophantine Equation) — Hilbert 第十问题

> 给定一个整系数多元多项式 p(x, y, z, ...),是否存在**整数解**使 p = 0?

- 例:x² − y² = 5 → 有解 (x=3, y=2)。
- Hilbert 在 1900 年将此列为 20 世纪要解决的重要问题。
- 1970 年代,Yuri Matiyasevich(以及 Martin Davis、Julia Robinson 的前期工作)证明:**不可判定**。

---

## 八、第二道分界线(下一讲预告)

即便在"可判定"内部,问题之间还有巨大差异:

- **能快速解决的** —— 多项式时间 (Polynomial,P)
- **不能快速解决的** —— 指数时间或更慢

下一讲将引入两个核心复杂度类 **P 与 NP**,讨论"快速可解"与"快速可验证"之间的根本区别。

---

## 九、关键术语速查

| 术语 | 含义 |
|---|---|
| Decision problem | 输出只有 Yes/No 的问题 |
| Language | 所有 Yes-输入构成的集合 |
| Turing machine | 通用的最简计算模型 |
| Church-Turing thesis | 所有可实现计算 ≡ 图灵机 |
| Decidable | 存在算法可解 |
| Undecidable | 不存在任何算法可解 |
| Halting problem | 判断程序是否死循环——不可判定 |
| Hilbert's 10th problem | 丢番图方程整数解存在性——不可判定 |

---

## 十、思维导图

```
计算理论
│
├── 把任意问题表达为"决策问题"
│     └── 答案 ∈ {Yes, No}
│
├── 决策问题 ↔ 语言 L ⊆ {0,1}*
│
├── 算法的统一模型 = 图灵机
│     └── Church-Turing 论题保证普适性
│
└── 所有问题
      ├── 可判定 (Decidable)
      │     ├── 容易 (P) ← 下一讲
      │     └── 困难 (Exp 等)
      │
      └── 不可判定 (Undecidable)
            ├── 停机问题
            └── 丢番图方程 (Hilbert 第十)
```
