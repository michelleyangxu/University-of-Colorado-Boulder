# 动态规划（Dynamic Programming）完整学习笔记

---

## 一、动态规划概述

动态规划是一种解决**优化问题**的算法设计技术，核心思想：

- 把大问题分解成子问题
- 子问题的解可以**重复利用**
- 通过最优子结构递推出全局最优解

> "Programming"在这里是**规划/调度**的意思，不是编程。

### 动态规划完整四步骤

```
Step 1：识别最优子结构
        ↓
Step 2：写出最优值的递推式
        ↓
Step 3：记忆化——建表，从小到大填表，避免重复计算
        ↓
Step 4：还原解——同步维护决策表S，回溯得到最优方案
```

---

## 二、问题一：钢条切割

### 2.1 问题定义

- 给定长度为 $L$ 的钢条
- 市场上每种长度有对应价格
- **目标：** 如何切割使总收益**最大化**（允许浪费）

### 2.2 递推式

定义 $T[i]$ = 长度为 $i$ 的钢条的最大收益：

```math
T[0] = 0
```

```math
T[i] = \max\left(0,\ T[i-l_1]+p_1,\ T[i-l_2]+p_2,\ \ldots\right)
```

- $i - l_k < 0$ 时，$T[i-l_k] = -\infty$（越界非法）
- $0$ 是浪费选项

### 2.3 填表示例（$L=10$，切3米\$1.80，切4米\$2.00）

| $i$ | $T[i]$ | $S[i]$（最优决策） |
|-----|--------|-----------------|
| 0 | 0 | nil |
| 1 | 0 | 浪费 |
| 2 | 0 | 浪费 |
| 3 | 1.80 | 切3米 |
| 4 | 2.00 | 切4米 |
| 6 | 3.60 | 切3米 |
| 10 | **5.60** | 切4米 |

**回溯：** $S[10]$=切4米 → $S[6]$=切3米 → $S[3]$=切3米 → 结束

**最优切法：4米 + 3米 + 3米，收益 $5.60**

### 2.4 与硬币找零的对比

| | 钢条切割 | 硬币找零 |
|--|---------|---------|
| 目标 | **最大化**收益 | **最小化**硬币数 |
| 允许浪费 | ✓ | ✗ |
| 越界惩罚值 | $-\infty$ | $+\infty$ |
| 表格维度 | 1维 | 1维 |

---

## 三、问题二：硬币找零

### 3.1 问题定义

- 给定硬币面值集合 $c_1, c_2, \ldots, c_n$
- 目标金额 $T$
- **目标：** 用**最少数量**的硬币凑出恰好 $T$（不允许浪费）

### 3.2 递推式

```math
\text{MinCoins}(0) = 0
```

```math
\text{MinCoins}(T) = +\infty \quad (T < 0)
```

```math
\text{MinCoins}(T) = \min_k\Big(1 + \text{MinCoins}(T - c_k)\Big)
```

### 3.3 填表示例（$T=40$，硬币={5, 20, 25}分）

| $i$ | $\text{Tbl}[i]$ | $S[i]$ |
|-----|----------------|--------|
| 0 | 0 | nil |
| 5 | 1 | 5分 |
| 20 | 1 | 20分 |
| 25 | 1 | 25分 |
| 40 | **2** | 20分 |

**回溯：** $S[40]$=20分 → $S[20]$=20分 → 结束

**最优方案：2个20分，共2枚**

---

## 四、通用DP模板（目标值分解类问题）

### 4.1 核心抽象

```python
@dataclass
class Action:
    cost:  int    # 每次操作消耗的目标量
    value: float  # 每次操作的收益/代价
    label: str    # 操作描述
```

### 4.2 三个关键参数

| 参数 | 钢条切割 | 硬币找零 |
|------|---------|---------|
| `cmp` 比较函数 | `a > b`（最大化） | `a < b`（最小化） |
| `allow_waste` | `True` | `False` |
| `penalty` 越界值 | $-\infty$ | $+\infty$ |

### 4.3 填表逻辑

```python
for i in range(1, T + 1):
    best = 0.0 if allow_waste else INF   # 初始值
    for action in actions:
        rem = i - action.cost
        prev_val = tbl[rem] if rem >= 0 else penalty
        if prev_val == penalty: continue  # 跳过非法路径
        candidate = prev_val + action.value
        if cmp(candidate, best):
            best = candidate
            s[i] = action
    tbl[i] = best
```

---

## 五、问题三：背包问题（0/1 Knapsack）

### 5.1 问题定义

- 背包容量 $W_0$，$n$ 个物品，各有重量 $w_i$ 和价值 $v_i$
- 每个物品**只能取一次**
- **目标：** 总重量 $\leq W_0$，总价值**最大化**

### 5.2 为什么需要二维表？

| | 钢条/找零 | 背包 |
|--|---------|------|
| 操作可重复？ | ✓ | ✗（每个物品只用一次） |
| 子问题维度 | 1维（剩余目标量） | **2维（物品编号 + 剩余容量）** |

### 5.3 递推式

定义 $T[j][W]$ = 考虑物品 $j$ 到 $n$，剩余容量 $W$ 时的最大价值：

```math
T[n+1][W] = 0 \quad \text{（物品用完）}
```

```math
T[j][0] = 0 \quad \text{（容量用完）}
```

```math
T[j][W] = \max\Big(T[j+1][W],\ v_j + T[j+1][W-w_j]\Big)
```

**最终答案：** $T[1][W_0]$

### 5.4 填表顺序（画法A：$j$ 从上到下递增）

```
        W=0  W=1  ...  W=W₀
j=1   [  0    ?   ...   ?  ]  ← 最后填，答案在此
j=2   [  0    ?   ...   ?  ]
...
j=n   [  0    ?   ...   ?  ]  ← 先填
j=n+1 [  0    0   ...   0  ]  ← 第一填（基础情形）
```

**填表方向：从下到上（$j$ 从 $n+1$ 到 $1$），从左到右（$W$ 从 $0$ 到 $W_0$）**

因为 $T[j][W]$ 依赖**下面一行**（$j+1$ 行）的值。

### 5.5 具体例子

3个物品，$W_0=5$：

| 物品 $j$ | 重量 | 价值 |
|---------|------|------|
| 1 | 2 | 3 |
| 2 | 3 | 4 |
| 3 | 1 | 2 |

填表结果：

```
        W=0  W=1  W=2  W=3  W=4  W=5
j=1   [  0    2    3    5    6    7  ]  ← 答案T[1][5]=7
j=2   [  0    2    2    4    6    6  ]
j=3   [  0    2    2    2    2    2  ]
j=4   [  0    0    0    0    0    0  ]  ← 基础情形
```

**最优方案：取物品1（价值3）+ 取物品2（价值4）= 7**

### 5.6 回溯S表

```
S[1][5] = 取  → 放入物品1，W = 5-2 = 3，移到j=2
S[2][3] = 取  → 放入物品2，W = 3-3 = 0，移到j=3
S[3][0] = 不取→ 容量为0，停止
```

### 5.7 时间复杂度

```math
T(n, W_0) = \Theta(n \cdot W_0)
```

**注意：这是伪多项式时间！** $W_0$ 增加1位数字，运行时间增加10倍，本质是指数级。这就是背包问题与 **P vs NP** 问题等价的原因。

---

## 六、动态规划的局限性

### 6.1 并非所有问题都有最优子结构

**反例1：变形背包**

价值计算方式改为：
```math
V = \begin{cases} \sum v_i & \text{取的物品数} < 10 \\ 0.1 \times \sum v_i & \text{取的物品数} \geq 10 \end{cases}
```

子问题的最优解依赖**已选物品数量**（未编码在参数里），最优子结构不成立。

**反例2：迷宫最长路径**

从 $(1,1)$ 到 $(n,n)$ 找最长路径（不能重复访问节点）。

子问题的最优解可能需要经过**已访问的节点**，但"已访问节点集合"没有编码在参数里，最优子结构不成立。

### 6.2 与普通背包的关键区别

| | 普通背包 | 迷宫最长路径 |
|--|---------|------------|
| 决策方向 | 单向（物品只往后走） | 双向（可以走回来） |
| 子问题会碰到前面的决策吗？ | ❌ | ✅ |
| 最优子结构 | ✅ | ❌ |

### 6.3 判断最优子结构的自查问题

1. 做出当前决策后，子问题形式是否和原问题完全相同？
2. 子问题的最优解是否**不依赖**之前的决策历史？
3. 子问题的最优解能否直接拼接到当前决策上？

三个都是"是"→ 有最优子结构；任何一个是"否"→ 需要重新思考。

---

## 七、问题四：最长公共子序列（LCS）

### 7.1 问题定义

- 给定两个字符串 $S_1$（长度 $m$）和 $S_2$（长度 $n$）
- **子序列**：不需要连续，但顺序不变
- **目标：** 找最长公共子序列

**应用：** 计算生物学中的基因序列比对。

### 7.2 决策方式

用两个指针 $i$、$j$ 分别指向 $S_1$ 和 $S_2$：

| 选项 | 条件 | 操作 |
|------|------|------|
| 匹配 | $S_1[i] = S_2[j]$ | 长度+1，移到 $(i+1, j+1)$ |
| 跳过 $S_1[i]$ | 任何时候 | 移到 $(i+1, j)$ |
| 跳过 $S_2[j]$ | 任何时候 | 移到 $(i, j+1)$ |

> **关键洞察：** 如果 $S_1[i] = S_2[j]$，**直接匹配永远不吃亏**。

### 7.3 递推式

定义 $\text{LCS}(i, j)$ = $S_1[i..m]$ 和 $S_2[j..n]$ 的最长公共子序列长度：

```math
\text{LCS}(m+1, j) = 0 \quad \text{（}S_1\text{用完）}
```

```math
\text{LCS}(i, n+1) = 0 \quad \text{（}S_2\text{用完）}
```

```math
\text{LCS}(i, j) = \begin{cases} 1 + \text{LCS}(i+1, j+1) & S_1[i] = S_2[j] \\ \max\Big(\text{LCS}(i+1, j),\ \text{LCS}(i, j+1)\Big) & S_1[i] \neq S_2[j] \end{cases}
```

**最终答案：** $\text{LCS}(1, 1)$

### 7.4 填表结构

```
        j=1  j=2  ...  j=n  j=n+1
i=1   [  ?    ?   ...   ?     0  ]  ← 答案在T[1][1]
i=2   [  ?    ?   ...   ?     0  ]
...
i=m   [  ?    ?   ...   ?     0  ]
i=m+1 [  0    0   ...   0     0  ]  ← 基础情形
```

**填表顺序：从右到左，从下到上**

因为 $T[i][j]$ 依赖：右边 $T[i][j+1]$、下面 $T[i+1][j]$、右下角 $T[i+1][j+1]$。

### 7.5 手推例子

```
S1 = A C T A    (m=4)
S2 = C A T      (n=3)
```

填表结果：

```
        j=1   j=2   j=3   j=4
        'C'   'A'   'T'   (终)
i=1 'A' [ 2     2     1     0  ]  ← 答案=2
i=2 'C' [ 2     1     1     0  ]
i=3 'T' [ 1     1     1     0  ]
i=4 'A' [ 1     1     0     0  ]
i=5 (终)[ 0     0     0     0  ]
```

**回溯：** LCS = `A T`，长度为2 ✓

### 7.6 回溯规则

| 标记 | 含义 | 移动方向 |
|------|------|---------|
| `match` | 字符匹配 | 斜下右 $(i+1, j+1)$，记录字符 |
| `right` | 跳过 $S_1[i]$ | 正下 $(i+1, j)$ |
| `down` | 跳过 $S_2[j]$ | 正右 $(i, j+1)$ |

### 7.7 时间复杂度

```math
T(m, n) = \Theta(m \cdot n)
```

---

## 八、四道题对比总结

```
切钢条          硬币找零         背包            LCS
  ↓               ↓              ↓              ↓
1维表格         1维表格         2维表格         2维表格
Tbl[0..L]      Tbl[0..T]      T[1..n+1][0..W₀] T[1..m+1][1..n+1]
最大化          最小化          最大化          最大化
允许浪费        不允许浪费      取或不取         匹配或跳过
Θ(n·L)         Θ(n·T)         Θ(n·W₀)        Θ(m·n)
```

---

## 九、最长稳定子序列（LSS）

### 9.1 问题定义

给定数组 $[a_0, a_1, \ldots, a_{n-1}]$，找最长子序列，要求相邻元素差 $\leq 1$。

**例子：** `[1, 4, 2, -2, 0, -1, 2, 3]` → `[1, 2, 2, 3]`，长度4。

### 9.2 递推式

定义 $\text{LSSLength}(i, a_j)$ = 从位置 $i$ 开始，上一个选择是 $a_j$ 时的最长稳定子序列长度：

```math
\text{LSSLength}(n, a_j) = 0 \quad \text{（数组扫完）}
```

```math
\text{LSSLength}(i, a_j) = \begin{cases} \text{LSSLength}(i+1, a_j) & a_j \neq \text{None},\ |a_i - a_j| > 1 \\ \max\Big(1 + \text{LSSLength}(i+1, a_i),\ \text{LSSLength}(i+1, a_j)\Big) & \text{否则} \end{cases}
```

### 9.3 表格结构

`T[(i, j)]` 的含义：从位置 $i$ 开始，**上一个已选的是 $a[j]$**，接下来最多能选多长。

- $j = -1$：特殊标记，表示还没选过任何元素（无约束）
- $j = 0, 1, \ldots, i-1$：上一个选的元素的真实下标

**填表顺序：** $i$ 从 $n-1$ 到 $0$（从下到上），$j$ 从 $-1$ 到 $i-1$

**最终答案：** $T[(0, -1)]$（从头开始，无约束）

### 9.4 回溯规则

```python
i, j = 0, -1
while i < n:
    if S[(i, j)] == 'take':
        result.append(a[i])
        j = i          # j 更新为 i
    i += 1             # i 始终前进
```

**关键：** 选了 $a[i]$ 后，$j$ 更新为 $i$，下一步查对应列；跳过时 $j$ 不变。

---

## 十、目标和问题（Target Sum）

### 10.1 问题定义

给定集合 $S$ 和目标 $N$，找子集 $T \subseteq S$：
- $\sum T \leq N$
- $N - \sum T$ 最小

### 10.2 递推式

```math
\text{targetSum}(S, i, \hat{T}) = \begin{cases} -\hat{T} & \hat{T} < 0 \\ \hat{T} & i > k \\ \min\Big(\text{targetSum}(S, i+1, \hat{T}-S[i]),\ \text{targetSum}(S, i+1, \hat{T})\Big) & \text{否则} \end{cases}
```

### 10.3 表格结构

`T[(i, j)]` = 从 $S[i]$ 开始选，剩余目标为 $j$ 时的最小差距：

- 基础情形：$T[(k, j)] = j$（元素用完，剩余差距就是 $j$）
- 越界（$j < S[i]$）时：强制跳过，不选该元素

**最终答案：** $T[(0, \text{tgt})]$

---

## 十一、常用速查

### 填表顺序总结

| 问题 | 表格维度 | 填表方向 | 答案位置 |
|------|---------|---------|---------|
| 钢条切割 | 1维 | 从小到大 | $T[L]$ |
| 硬币找零 | 1维 | 从小到大 | $T[\text{tgt}]$ |
| 背包 | 2维 | 从下到上，从左到右 | $T[1][W_0]$ |
| LCS | 2维 | 从右到左，从下到上 | $T[1][1]$ |
| LSS | 2维（字典） | $i$ 从大到小 | $T[(0,-1)]$ |

### 越界惩罚值

| 问题类型 | 越界惩罚 |
|---------|---------|
| 最大化问题（钢条、背包） | $-\infty$ |
| 最小化问题（找零、目标和） | $+\infty$ |

### DP四步口诀

```
子结构 → 递推式 → 填表 → 回溯
```
