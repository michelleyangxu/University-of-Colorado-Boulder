# 傅里叶变换与FFT 完整学习笔记

---

## 一、什么是傅里叶变换？

### 1.1 信号的定义

信号是随时间变化的数值序列，例如：
- 股票/商品价格
- 某地温度
- 飞机遇到的风速
- 语音/音乐录音

**离散信号**：在固定采样间隔 $\Delta t$ 下记录值，写作：

```math
a_0, a_1, a_2, \ldots, a_{n-1}
```

假设信号是**周期性的**（观测到一个周期后无限重复）。

### 1.2 傅里叶变换的核心思想

> 任何信号都可以写成**不同频率的正弦波之和**。

- **时域**（输入）：$a_0, a_1, \ldots, a_{n-1}$（随时间变化的值）
- **频域**（输出）：$A_0, A_1, \ldots, A_{n-1}$（各频率分量的幅度）

**应用场景：** 语音处理、图像处理、时间序列去噪、季节性趋势分析等。

---

## 二、复数基础

### 2.1 复数的两种表示

**直角坐标形式：**

```math
z = x + yj, \quad j = \sqrt{-1}
```

- **实部**：$x$，**虚部**：$y$
- **模（长度）**：$r = \sqrt{x^2 + y^2}$
- **相位（角度）**：$\theta = \arctan(y/x)$（已知对边/邻边之比，反推角度）

**极坐标形式（欧拉公式）：**

```math
z = re^{j\theta}, \quad e^{j\theta} = \cos\theta + j\sin\theta
```

两种形式完全等价：
- **加法**时用直角坐标更方便（实部虚部直接相加）
- **乘法**时用极坐标更方便（模相乘，角度相加）

### 2.2 复数乘法规则

```math
z_1 \times z_2 = r_1 e^{j\theta_1} \cdot r_2 e^{j\theta_2} = (r_1 r_2)\, e^{j(\theta_1 + \theta_2)}
```

- **模相乘**：$|z_1 z_2| = r_1 r_2$（缩放）
- **角度相加**：$\angle(z_1 z_2) = \theta_1 + \theta_2$（旋转）

> 复数乘法的本质：**旋转 + 缩放**。

### 2.3 欧拉恒等式

```math
e^{j\pi} + 1 = 0
```

**推导：**

```math
e^{j\pi} = \cos(\pi) + j\sin(\pi) = -1 + 0 = -1
```

几何意义：从 1 出发，在单位圆上逆时针旋转 180°，到达 $-1$。

把数学中五个最重要的常数 $e,\ j,\ \pi,\ 1,\ 0$ 联系在一起。

### 2.4 复数的共轭

复数 $z = x + yj$ 的共轭：

```math
z^* = x - yj
```

把虚部符号取反，在复平面上是关于**实轴的镜像**。对于单位根：$\omega_n^* = \omega_n^{-1}$（共轭 = 取倒数）。

---

## 三、单位根（Roots of Unity）

### 3.1 定义

$\omega$ 是 $n$ 次单位根，若满足：

```math
\omega^n = 1
```

### 3.2 推导

令 $\omega = re^{j\theta}$，则：

```math
\omega^n = r^n e^{jn\theta} = 1
```

由于 $1 = 1 \cdot e^{j \cdot 0}$，用欧拉公式展开 $e^{jn\theta} = 1$：

```math
\cos(n\theta) = 1, \quad \sin(n\theta) = 0
\quad \Rightarrow \quad n\theta = 2\pi k
```

因此：

```math
r = 1, \quad \theta = \frac{2\pi k}{n}, \quad k = 0, 1, 2, \ldots, n-1
```

> ⚠️ 注意：不能直接"取对数"得 $jn\theta = 2\pi$，因为 $e^{jx}$ 是三角函数，必须通过欧拉公式展开后令实部虚部分别相等。

### 3.3 n 个单位根

```math
\omega_n^k = e^{j \cdot \frac{2\pi k}{n}}, \quad k = 0, 1, \ldots, n-1
```

- 在复平面上均匀分布在单位圆上
- 相邻夹角为 $2\pi/n$
- **生成元** $\omega_n = e^{j \cdot 2\pi/n}$，所有单位根都是它的幂次

### 3.4 四个关键性质

**性质 1：转满 n 步回到原点**

```math
\omega_n^n = e^{j \cdot 2\pi} = 1
```

**性质 2：中间根等于 −1（n 为偶数时）**

```math
\omega_n^{n/2} = e^{j\pi} = -1
```

**性质 3：旋转半圈变负号**

```math
\omega_n^{k+n/2} = \omega_n^k \cdot \omega_n^{n/2} = \omega_n^k \cdot (-1) = -\omega_n^k
```

**性质 4：折叠性质（FFT 的核心）**

```math
\left(\omega_n^i\right)^2 = \left(e^{j \cdot \frac{2\pi i}{n}}\right)^2 = e^{j \cdot \frac{2\pi i}{n/2}} = \omega_{n/2}^i
```

### 3.5 举例

**3 次单位根**（夹角 120°）：

| $k$ | $\omega_3^k$ |
|-----|-------------|
| 0 | $1$ |
| 1 | $-\dfrac{1}{2} + \dfrac{\sqrt{3}}{2}j$ |
| 2 | $-\dfrac{1}{2} - \dfrac{\sqrt{3}}{2}j$ |

**8 次单位根**（夹角 45°）：

```math
1,\quad \frac{1}{\sqrt{2}}+\frac{1}{\sqrt{2}}j,\quad j,\quad -\frac{1}{\sqrt{2}}+\frac{1}{\sqrt{2}}j,\quad -1,\quad \ldots
```

---

## 四、离散傅里叶变换（DFT）

### 4.1 信号的多项式表示

给定信号 $a_0, a_1, \ldots, a_{n-1}$，构造多项式：

```math
a(x) = a_0 + a_1 x + a_2 x^2 + \cdots + a_{n-1} x^{n-1}
```

把信号值当作多项式的**系数**，$x$ 是占位符。

### 4.2 DFT 的定义

**DFT = 把多项式 $a(x)$ 在所有单位根处求值：**

```math
A_k = a(\omega_n^k) = \sum_{i=0}^{n-1} a_i \cdot \omega_n^{ki}, \quad k = 0, 1, \ldots, n-1
```

展开每一项（代入欧拉公式）：

```math
A_k = \sum_{i=0}^{n-1} a_i \cdot \left(\cos\frac{2\pi ki}{n} + j\sin\frac{2\pi ki}{n}\right)
```

这就是用第 $k$ 个频率的正余弦去"探测"信号中有多少该频率的成分。

> **两种视角，同一个计算：**
> - 多项式视角：把 $\omega_n^k$ 代入 $a(x)$，计算多项式的值
> - 傅里叶视角：检测频率 $k$ 的分量强度

### 4.3 逆离散傅里叶变换（IDFT）

```math
a_i = \frac{1}{n} \sum_{k=0}^{n-1} A_k \cdot \omega_n^{-ki}
```

- 使用**共轭单位根** $\omega_n^{-k}$（顺时针旋转，方向相反）
- 除以 $n$ 是必须的归一化步骤
- DFT 和 IDFT 互为逆运算

### 4.4 朴素 DFT 的复杂度

每次求值 $\Theta(n)$，共 $n$ 个点：

```math
\text{朴素 DFT} = \Theta(n^2)
```

---

## 五、快速傅里叶变换（FFT）

### 5.1 核心思想：奇偶分解

把多项式按奇偶系数拆分：

```math
a(x) = \underbrace{(a_0 + a_2x^2 + \cdots + a_{n-2}x^{n-2})}_{a_{\text{even}}(x^2)}
     + x \cdot \underbrace{(a_1 + a_3x^2 + \cdots + a_{n-1}x^{n-2})}_{a_{\text{odd}}(x^2)}
```

### 5.2 递推公式推导

代入 $x = \omega_n^i$，利用折叠性质 $(\omega_n^i)^2 = \omega_{n/2}^i$，认出子 DFT：

```math
A_i = A_{\text{even},i} + \omega_n^i \cdot A_{\text{odd},i}
```

利用性质 $\omega_n^{k+n/2} = -\omega_n^k$，得对称公式（只需循环 $n/2$ 次）：

```math
A_k = A_{\text{even},k} + \omega_n^k \cdot A_{\text{odd},k}
```

```math
A_{k+n/2} = A_{\text{even},k} - \omega_n^k \cdot A_{\text{odd},k}
```

设 $t = \omega_n^k \cdot A_{\text{odd},k}$，一加一减同时得到两个系数，避免重复计算。

### 5.3 FFT 算法结构

```
FFT(a[0..n-1]):
  if n == 1: return a[0]              ← 基础情形

  a_even = a[0], a[2], a[4], ...     ← 偶数下标
  a_odd  = a[1], a[3], a[5], ...     ← 奇数下标

  A_even = FFT(a_even)               ← 递归，规模 n/2
  A_odd  = FFT(a_odd)                ← 递归，规模 n/2

  for k = 0 to n/2 - 1:
    t          = ωₙᵏ · A_odd[k]
    A[k]       = A_even[k] + t       ← 合并
    A[k + n/2] = A_even[k] - t

  return A
```

**要求：** $n$ 必须是 2 的幂次。

### 5.4 时间复杂度

递推式：

```math
T(n) = 2T(n/2) + \Theta(n)
```

$a=2,\ b=2,\ c=1$，$\log_b a = 1 = c$，主定理 Case 2：

```math
T(n) = \Theta(n\log n)
```

---

## 六、FFT 应用一：多项式乘法

### 6.1 问题

给定两个 $n-1$ 次多项式 $a(x)$，$b(x)$，求乘积 $c(x) = a(x) \cdot b(x)$。

- 朴素算法：$\Theta(n^2)$
- FFT 算法：$\Theta(n\log n)$

### 6.2 核心观察

在任意点 $x_0$ 处，乘法退化为普通数的乘法：

```math
c(x_0) = a(x_0) \cdot b(x_0)
```

### 6.3 完整流程

```
系数 a, b
  ↓ 补零到长度 2n（c 有 2n-1 个系数）
  ↓ FFT（在 2n 个单位根处求值）  Θ(n log n)
点值 A, B
  ↓ 逐点相乘 Cᵢ = Aᵢ · Bᵢ         Θ(n)
点值 C
  ↓ IDFT 还原系数                  Θ(n log n)
系数 c[0..2n-2]
```

> **为什么补零？** $c(x)$ 是 $2n-2$ 次多项式，需要至少 $2n-1$ 个点才能唯一确定，所以两个多项式都补零到长度 $2n$。

### 6.4 手推例子

令 $a(x) = 1 + 2x$，$b(x) = 3 + x$，期望结果 $c(x) = 3 + 7x + 2x^2$。

补零后使用 4 次单位根 $\omega_4^k = j^k$：

**求值：**

```math
A_0 = 3,\quad A_1 = 1+2j,\quad A_2 = -1,\quad A_3 = 1-2j
```

```math
B_0 = 4,\quad B_1 = 3+j,\quad B_2 = 2,\quad B_3 = 3-j
```

**逐点相乘：**

```math
C_0 = 12,\quad C_1 = 1+7j,\quad C_2 = -2,\quad C_3 = 1-7j
```

**IDFT 还原（除以 4）：**

```math
c_0 = 3,\quad c_1 = 7,\quad c_2 = 2,\quad c_3 = 0 \quad \checkmark
```

### 6.5 时间复杂度

| 步骤 | 复杂度 |
|------|--------|
| 补零 | $\Theta(n)$ |
| FFT（$a$ 和 $b$） | $\Theta(n\log n)$ |
| 逐点相乘 | $\Theta(n)$ |
| IDFT | $\Theta(n\log n)$ |
| **总计** | $\Theta(n\log n)$ |

---

## 七、FFT 应用二：信号处理

### 7.1 频率解释

给定采样间隔 $\Delta t$，序列长度 $n$，第 $i$ 个 FFT 系数对应频率：

```math
f_i = \frac{i}{n \cdot \Delta t}
```

- $A_0$：直流分量（信号均值，0 频率）
- $A_1$ 到 $A_{n/2}$：正频率（有意义的部分）
- $A_{n/2+1}$ 到 $A_{n-1}$：负频率（与正频率共轭，不含新信息）

### 7.2 频率对称性

对实数序列做 FFT，结果具有共轭对称性：

```math
A_k \text{ 和 } A_{n-k} \text{ 互为复共轭}
```

只需分析 $A_0$ 到 $A_{n/2}$。

### 7.3 低频滤波（平滑信号）

保留前 $k$ 个低频系数，其余置零，再做 IDFT：

- 保留越少的低频 → 信号越平滑，只看大趋势
- 保留越多的频率 → 信号越接近原始数据
- **残差**（原始 - 低频重建）= 高频波动（短期噪声）

### 7.4 实际案例

**原油价格（$n=1024$ 周，$\Delta t = 1$ 周）：**

| 保留频率数 | 对应周期 | 效果 |
|-----------|---------|------|
| 1 个 | 20 年 | 20 年大趋势 |
| 3 个 | 7~20 年 | 长期趋势 |
| 20 个 | 1 年以上 | 年度波动 |

**音频信号：**

| 频率范围 | 听到的内容 |
|---------|-----------|
| 0 ~ 200 Hz | 极低频，几乎听不见 |
| 0 ~ 1000 Hz | 低音 / bass |
| 1000 ~ 3000 Hz | 小提琴低频部分 |
| 3000 ~ 15000 Hz | 小提琴高频（明亮感） |

### 7.5 FFT 的核心用途

| 用途 | 原理 |
|------|------|
| 平滑 / 去噪 | 保留低频，丢弃高频噪声 |
| 频率分析 | 看哪些频率分量幅度大 |
| 季节性分解 | 找出周期性趋势 |
| 音频处理 | 按频段分离、滤波 |
| 图像处理 | 二维 FFT，压缩、锐化 |
| 多项式乘法 | 时域卷积 = 频域逐点相乘 |

---

## 八、知识点总览

```
信号 a₀, a₁, ..., aₙ₋₁
        ↓ 构造多项式 a(x)
        ↓ 在单位根 ω⁰, ω¹, ..., ωⁿ⁻¹ 处求值
   DFT：A₀, A₁, ..., Aₙ₋₁  （频域）
        ↓ IDFT（共轭单位根，除以 n）
   还原：a₀, a₁, ..., aₙ₋₁  （时域）

FFT 加速求值：Θ(n²) → Θ(n log n)
原理：奇偶分解 + 折叠性质 (ωₙⁱ)² = ω_{n/2}ⁱ
```

---

## 九、常用恒等式速查

| 公式 | 内容 |
|------|------|
| 欧拉公式 | $e^{j\theta} = \cos\theta + j\sin\theta$ |
| 欧拉恒等式 | $e^{j\pi} + 1 = 0$ |
| 单位根定义 | $\omega_n^k = e^{j \cdot 2\pi k/n}$ |
| 折叠性质 | $(\omega_n^i)^2 = \omega_{n/2}^i$ |
| 对称性质 | $\omega_n^{k+n/2} = -\omega_n^k$ |
| 对数恒等式 | $a^{\log_b n} = n^{\log_b a}$ |
| 复数乘法 | 模相乘，角度相加 |
