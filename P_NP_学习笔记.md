# P 类与 NP 类问题 — 学习笔记

> 整理自两节课的字幕内容(Lecture 20 + 21)与配套答疑

---

## 目录

1. [问题的难度层级](#一问题的难度层级)
2. [什么叫"快"——多项式时间的约定](#二什么叫快多项式时间的约定)
3. [P 类(Polynomial Time)](#三p-类polynomial-time)
4. [NP 类(Nondeterministic Polynomial Time)](#四np-类nondeterministic-polynomial-time)
5. [P 与 NP 的关系](#五p-与-np-的关系)
6. [NP 的形式定义](#六np-的形式定义)
7. [三个经典 NP 问题](#七三个经典-np-问题)
8. ["非确定性"是什么意思](#八非确定性是什么意思)
9. [深入答疑:容易混淆的关键点](#九深入答疑容易混淆的关键点)

---

## 一、问题的难度层级

复杂性理论中,**三个层次**的难度经常被混淆:

| 概念 | 含义 | 例子 |
|---|---|---|
| **Undecidable** | 根本**没有任何算法**能解决,无论给多少时间 | 停机问题、Hilbert 第十问题 |
| **可判定但慢** | **有算法**,但目前没有快速(多项式)算法 | 因数分解、最长简单路径 |
| **P** | **有多项式时间算法** | 素性测试、最短路径、排序 |

整体结构图:

```
所有计算问题
│
├─ Undecidable(不可判定)
│   • 停机问题
│   • Hilbert 第十问题
│   ▲ 没有任何算法能解决
│
└─ Decidable(可判定)
    │
    ├─ 已知指数下界(EXPTIME-hard)
    │
    ├─ NP
    │   │
    │   ├─ P(已知多项式)
    │   │   • 排序、最短路径、素性测试、MST
    │   │
    │   └─ NP \ P(疑似不在 P 中,但未证明)
    │       • 因数分解
    │       • 最长简单路径
    │       • 哈密顿环、3-着色、SAT、TSP
    │
    └─ ...
```

---

## 二、什么叫"快"——多项式时间的约定

"快"本身没有数学定义,是**计算机科学界达成的共识**:以**输入规模 n** 为基准,运行时间是 n 的**多项式**(O(n^c),c 为常数)就算快。

| 时间复杂度 | 是否算快 |
|---|---|
| θ(log n) | 快 |
| θ(n)、θ(n log n) | 快 |
| θ(n²)、θ(n^1.54)(Karatsuba) | 快 |
| θ(2^n) | 慢 |

### 关键陷阱:输入规模 = 编码长度

**输入规模指的是编码长度(位数),不是数值大小。**

数字 n 的输入规模:

$$k = \lceil \log_2(n+1) \rceil \approx \log_2 n \text{ 位}$$

为什么用二进制?其实用十进制也行——它们只差一个常数倍(log₂(n) = log₁₀(n) × 3.32),在大 O 记号里被吸收。

### 试除法的"伪装"

试除法判断 n 能否分解:

```
for i = 2 to √n:
    if n % i == 0: 找到因子
```

乍一看 √n 不大,但代入 n = 2^k:

$$\sqrt{n} = \sqrt{2^k} = 2^{k/2}$$

**这是指数时间!** 课堂实验:每多 2 位,分解时间翻倍——因为 T(k+2) = 2 · T(k)。

### 为什么多项式时间被选作"快"的边界

虽有争议但被接受:
- 理论上 n^100 也算多项式,确实不快
- 但实践中,绝大多数有用的多项式算法指数都很小(1、2、3 左右)
- **多项式有封闭性**:相加、相乘、嵌套都还是多项式——形成稳定的复杂度阶层

---

## 三、P 类(Polynomial Time)

**定义**:存在 O(n^c) 算法的所有判定问题集合。

### P 类问题例子

- **图的 s-t 连通性**:DFS,O(V+E)
- **大数乘法的高位判定**:Karatsuba,O(n^1.54)
- **图是否有环**:DFS 检查回边
- **素性测试(是否为素数)**:AKS 算法(Agrawal–Kayal–Saxena, 2002),多项式时间
- **数组中位数是否 ≤ 0**:线性时间求中位数即可
- **最短路径(权重 ≤ W)**:Dijkstra / Bellman-Ford
- **最小生成树**:Kruskal / Prim
- **负权环检测**:Bellman-Ford,O(V·E)

### 关键对比:素性测试 vs 因数分解

| 问题 | 输入 | 问什么 | 复杂度 |
|---|---|---|---|
| **素性测试** | n | n 是不是素数?(yes/no) | **P**(AKS,2002) |
| **因数分解** | n | n 的因子是什么? | **未知是否 P** |

**关键洞察**:
- 素性测试:只问"有没有"非平凡因子,**可以靠代数性质(费马小定理等)绕过"找因子"**
- 因数分解:必须**找出**因子,目前没有捷径

**类比**:判断"是不是大象" vs "描述大象的具体结构"——前者容易,后者难。

---

## 四、NP 类(Nondeterministic Polynomial Time)

### 核心直觉

> **NP 的本质:答案不一定好求,但好检验。**

一个问题 ∈ NP,如果:
- yes 答案 → 存在一个**简短证书(certificate)**
- 验证证书 → **多项式时间**内完成

### 数独类比

- 解一个数独:很难,要花很长时间想
- 检查别人填好的数独:很简单,几分钟扫一遍

**"难解、易查"——这就是 NP 的味道。**

### 证书是什么?

证书 = **支撑 yes 答案的具体证据**

| 概念 | 内容 | 形式 |
|---|---|---|
| **答案** | yes / no | 一个布尔值 |
| **证书** | 具体的对象(数、路径、赋值……) | 让人能快速确认 yes 的物证 |

**常见模式:NP 问题的证书 = 答案对应的具体对象本身**

| 问题 | 证书 |
|---|---|
| 图有哈密顿环吗? | 那个环(一串顶点) |
| 背包问题(有解吗?) | 那一组物品 |
| s-t 最短路径(≤W)? | 那条路径 |
| 数独有解吗? | 那个填好的解 |
| SAT 可满足吗? | 让公式为真的变量赋值 |
| 3-着色可行吗? | 顶点的颜色分配 |

### 关键不对称性:NP 只管 yes 答案

| 答案 | 是否需要证书? |
|---|---|
| **yes** | ✅ 必须能用简短证书证明 |
| **no** | ❌ NP 不要求 |

**对应的 co-NP** 专门处理 no 方向的证书:

- **NP**:"yes 的辩护方"——你说有解,请举证给我看
- **co-NP**:"no 的辩护方"——你说无解,请举证给我看

例子:
- **Composite(是否为合数)**:yes 证书 = 一个因子 → **∈ NP**
- **Prime(是否为素数)**:no 证书 = 一个因子 → **∈ co-NP**

---

## 五、P 与 NP 的关系

### P ⊆ NP(几乎是定义直接推出)

如果一个问题已经能在多项式时间内**求解**,那么:
- 证书可以是空的
- 直接重新跑一遍可信的多项式算法验证即可
- 因此一定属于 NP

### P = NP? — 60 年悬而未决,百万美元千禧难题

是否存在问题 ∈ NP 但 ∉ P?**没人知道**。

两种可能:

```
情况 A:P = NP            情况 B:P ≠ NP

   ┌──────────┐              ┌──────────┐
   │    NP    │              │    NP    │
   │ ┌──────┐ │              │  ┌────┐  │
   │ │ P=NP │ │   ←→         │  │ P  │  │
   │ └──────┘ │              │  └────┘  │
   └──────────┘              └──────────┘
   两个圈重合                  P 真的更小
```

大多数计算机科学家**猜**是 B(P ≠ NP),但没人能证明。

### 候选"嫌疑犯"

- **因数分解**:证书 = 其中一个因子,但目前没有多项式算法
- **最长简单路径**:加上"simple(不重复)"约束几乎总让问题变难
- **3-着色、SAT、TSP**:NP-complete 的经典代表

**有趣对比**:仅仅把"短"改成"长",难度就跳跃:

| 问题 | 复杂度 |
|---|---|
| 最**短**路径(≤ W) | **P**(Dijkstra) |
| 最**短**生成树 | **P**(Kruskal/Prim) |
| 最**长**简单路径 | **NP-hard** |
| 最**长**简单环(Hamilton) | **NP-complete** |

---

## 六、NP 的形式定义

一个问题 ∈ NP,**当且仅当**存在一个**证书算法(certifier)**,满足:

对所有输入 x:

| 情况 | 条件 |
|---|---|
| **x 真实答案是 yes** | **存在**证书 y,使得:① \|y\| 是 \|x\| 的多项式;② certifier(x, y) 返回 yes |
| **x 真实答案是 no** | **不存在**任何证书 y 能让 certifier(x, y) 返回 yes |

并且:**certifier 本身在多项式时间内运行**。

### 三个核心要素

1. **证书的长度**:相对于输入是多项式的(不能膨胀)
2. **验证器的运行时间**:多项式时间
3. **不可伪造性**:no 实例下,没有任何证书能骗过验证器

---

## 七、三个经典 NP 问题

### 1. 图 3-着色(3-Coloring)

**问题**:给定无向图 G,能否用红/绿/蓝三色给每个顶点染色,使得**没有任何一条边的两端同色**?

**例子**:

不可 3 着色的经典图 K₄(4 个顶点两两相连):
```
        4
       /|\
      / | \
     /  |  \
    1---2---3
```
顶点 4 与 1、2、3 都相邻,但只有 3 种颜色——无解。

**NP 证书**:
- 证书:每个顶点的颜色分配
- 大小:O(n)
- 验证:① 每个顶点颜色合法;② 每条边两端不同色 → O(V+E)

**有趣事实**:
- **2-着色**:**P**(等价于二分图判定,DFS 即可)
- **3-着色**:**NP-complete**
- 从 2 到 3 这一步,难度跳跃巨大

### 2. 布尔可满足性问题(SAT)

**问题**:给定 CNF 布尔公式,是否存在变量赋值使公式为真?

**CNF 格式**:**先 OR 后 AND**

$$\underbrace{(x_1 \lor x_2 \lor \neg x_3)}_{\text{子句 1}} \land \underbrace{(x_1 \lor \neg x_2 \lor x_3)}_{\text{子句 2}} \land \underbrace{(x_4 \lor x_3 \lor \neg x_5)}_{\text{子句 3}}$$

- **子句(clause)**:子句内部用 OR
- **整个公式**:子句之间用 AND
- 公式为真 ⟺ **每个子句都为真**(因为是 AND)
- 子句为真 ⟺ **至少一个文字为真**(因为是 OR)

**生活类比**:组团出游投票——每个人提出一条要求(子句,OR),所有人都必须满意(公式,AND)。

**NP 证书**:
- 证书:变量的真值赋值
- 大小:O(n)(n 为变量数)
- 验证:代入求值,O(公式长度)

**SAT 的特殊地位**:**第一个被证明的 NP-complete 问题**(Cook 定理,1971)。如果 SAT 有多项式算法,则 P = NP。

### 3. 旅行商问题(TSP)

**问题**:加权图 G 和预算 B。能否找到一条**回路**,**恰好访问每个顶点一次**,返回起点,且总权重 ≤ B?

**实际背景**:FedEx、UPS、Amazon 配送优化——求解能节省巨额成本。

**NP 证书**:
- 证书:顶点访问序列 (v₁, v₂, ..., v_n, v₁)
- 大小:O(V)
- 验证:① 每个顶点恰好访问一次;② 起点 = 终点;③ 总权重 ≤ B

### 统一模式

三个问题的结构**完全相似**:

| 问题 | 证书 | 验证 |
|---|---|---|
| **3-着色** | 颜色分配 | 是合法 3 种颜色 + 边两端不同色 |
| **SAT** | 变量赋值 | 代入后公式为 true |
| **TSP** | 顶点序列 | 是合法路径 + 不重复 + 满足预算 |

---

## 八、"非确定性"是什么意思

### 想象一台"会猜的计算机"

普通计算机每一步只能确定地执行。**非确定性计算机**在每一步可以**神奇地猜一个 bit**(0 或 1),仿佛能"看见"未来。

工作流程:
1. 看到输入 x
2. **猜出**一个证书 y(每一位都猜对)
3. 跑验证器 certifier(x, y)
4. 如果返回 yes → 整体 yes
5. 如果**所有可能猜测**都让验证器返回 no → 整体 no

### Oracle 类比

- 你问神谕:"我的 TSP 实例有解吗?"
- 神谕**立即给你一条路径**作为证据
- 你验证 → 是 → 答案 yes
- 神谕沉默或乱给 → 答案 no(全能神谕找不到 = 不存在)

**NP = 这种"会猜计算机"上的多项式时间** —— 这就是 "**N**ondeterministic **P**olynomial" 名字的由来。

### 现实里有这种计算机吗?

- **经典计算机**:**没有**。模拟方法只有穷举所有可能证书,2^|y|
- **量子计算机**:**部分地**具有类似能力(下节内容)

### 推论:NP ⊆ EXPTIME

每个 NP 问题都有指数时间算法(枚举所有证书逐一验证)。

$$P \subseteq NP \subseteq \text{EXPTIME}$$

---

## 九、深入答疑:容易混淆的关键点

### 1. 多项式时间是不是"指数时间"?

**这是个常见的术语混淆**:

| 表达式 | 谁是底,谁是指数? | 类型 |
|---|---|---|
| $n^c$($c$ 是常数,如 $n^3$) | **底是变量 n**,指数固定 | **多项式** |
| $c^n$($c$ 是常数,如 $2^n$) | 底固定,**指数是变量 n** | **指数函数** |

**核心区别**:
- $n^c$:**变量在底,指数固定** → 多项式增长
- $c^n$:**变量在指数,底固定** → 指数爆炸

**严格事实**:对任意常数 $a > 1$ 和 $c > 0$:

$$\lim_{n \to \infty} \frac{n^c}{a^n} = 0$$

再大的多项式 $n^{1000000}$,最终都会被 $1.000001^n$ 远远甩在后面。

**复杂度阶梯**:

| 类型 | 示例 | 名称 |
|---|---|---|
| 常数 | O(1) | 常数时间 |
| 对数 | O(log n) | 对数时间 |
| 多项式 | O(n), O(n²), O(n^1000) | **多项式时间(P)** |
| 亚指数 | O(2^√n) | 亚指数时间 |
| **指数** | O(2^n), O(3^n) | **指数时间** |
| 阶乘 | O(n!) | 阶乘时间 |

### 2. 因数分解是 undecidable 吗?

**不是!** 因数分解是 **decidable(可判定)** 的——试除法就能解决,只是慢。

**三个层次绝不能混淆**:
- 停机问题、Hilbert 第十问题:**undecidable**(完全无解)
- 因数分解:**decidable 但慢**(没有已知多项式算法)
- 排序、最短路径:**P**(快)

### 3. 输入规模为什么是位数?

因为算法的每一步至少要"读一眼"输入。输入需要 k 位才能写下,读完就要 k 步。

**核心思想**:把整个输入写到纸上,总共要写多少个符号,就是输入规模。

| 输入类型 | 输入规模通常指 |
|---|---|
| 一个整数 n | 二进制位数 ≈ log n |
| 一个数组 | 元素个数 |
| 一个图 G | 顶点数 V + 边数 E |

### 4. 为什么 NP 证书只对 yes 答案要求?

NP 描述的是"**靠运气能多快找到解**":

- **yes 实例**:有解 → 存在一组正确的"幸运猜测" → 存在证书
- **no 实例**:无解 → 怎么猜都没用 → 不存在能"猜出"的证书

**反例不能证明不存在**:背包问题中,给出"一组重量 ≤ W 价值 < V 的物品"不能证明"不存在重量 ≤ W 价值 ≥ V 的选择"——可能还有别的选择满足条件。

### 5. 为什么 P ⊆ NP "几乎是平凡的"?

如果问题本身已经能多项式时间求解,**根本不需要证书**:
- 证书可以是空的
- 拿到输入,直接用可信的实现重新算一遍
- 整个过程仍是多项式时间

### 6. 求 p 的第 k 位是什么意思?

**这是把求值问题转为判定问题的标准技巧**:

- 原问题(求值):"分解 n,返回 p" → 不是 yes/no,不符合 NP 格式
- 改造(判定):"p 的第 k 位是 1 吗?" → yes/no 形式

如果能多项式回答"任意第 k 位",就能逐位重建完整的 p → **与分解等价难**。

**例子**:n = 143 = 11 × 13,p = 11(二进制 1011)
- "p 的第 0 位是 1 吗?" → yes
- "p 的第 2 位是 1 吗?" → no

证书 = p 本身。验证:① p 整除 n;② p² ≤ n;③ 读 p 的第 k 位。

### 7. TSP 为什么是 NP?这和 DP 有关系吗?

**TSP ∈ NP** 是因为**验证容易**,**和 DP 无关**:
- 给一条路径,O(n) 验证

**TSP 难解(疑似 ∉ P)** 才和 DP 相关:
- 决策时需要记住"已访问的顶点集合"
- DP 状态空间至少 O(2^n · n)(Held-Karp 算法)
- 仍是指数级

**注意两层独立的论证**:
- NP 看**验证**:与 DP 无关
- P 看**求解**:DP 能否多项式工作

### 8. 约束纠缠为什么让 DP 失效?

**DP 工作的根本前提**:**最优子结构** —— 子问题的答案可以用少量状态变量总结。

| 问题 | 状态需要记录 | DP 是否有效 |
|---|---|---|
| 最短路径 | 到达 v 的距离(一个数) | ✓ 多项式 |
| TSP | 走过的顶点集合 + 当前位置 | 指数(O(2^n · n)) |
| 3-着色 | 所有已涂色顶点的颜色 | 指数(O(3^n)) |

**关键差异**:
- 最短路径:**到达 v 的路径细节不影响未来决策** → 状态可压缩
- 3-着色:**已涂色顶点的颜色持续影响未来约束** → 状态不可压缩

**有趣事实**:许多 NP-hard 问题**在树上变为多项式**(独立集、Hamilton 路径、3-着色……),因为树没有环,子问题真正独立。

---

## 十、关键概念速查

| 术语 | 含义 |
|---|---|
| **P** | 多项式时间可求解的判定问题 |
| **NP** | 多项式时间可验证 yes 答案的判定问题 |
| **co-NP** | 多项式时间可验证 no 答案的判定问题 |
| **EXPTIME** | 指数时间可求解的问题 |
| **decidable** | 有算法能解决(不论多慢) |
| **undecidable** | 不存在任何算法 |
| **certificate(证书)** | 用来快速验证 yes 答案的简短证据 |
| **certifier(验证器)** | 多项式时间的验证算法 |
| **CNF** | 合取范式,SAT 的标准格式 |
| **nondeterministic** | 假想的"会猜"的计算机模型 |

---

## 十一、记忆要点

1. **输入规模是位数,不是数值** —— 试除法的"伪装"陷阱
2. **多项式 vs 指数**:看变量在底还是在指数位置
3. **P ⊆ NP 是平凡的;P = NP? 是世纪难题**
4. **NP 证书只对 yes 答案要求** —— 不对称性
5. **证书 = 答案对应的具体对象本身**(路径、赋值、颜色……)
6. **素性 ∈ P,因数分解 P 归属未知** —— 两者本质不同
7. **NP 看验证,P 看求解** —— 这是两层独立的论证
8. **"短"通常容易,"长且简单"通常困难**
9. **DP 失效是因为状态不可压缩** —— 不代表问题没法属于 NP
10. **3-Coloring、SAT、TSP** 是 NP-complete 的三大经典代表

---

## 下节预告:NP-completeness

- NP 内部存在一类"最难的问题"
- 任意一个有多项式算法 ⟹ **整个 NP = P**
- 3-着色、SAT、TSP 都是 NP-complete —— 它们的难度本质上是同一件事
- **归约(reductions)**:在问题间建立"难度桥梁"的工具
