# 量子比特与量子操作 · 学习重点整理

> 上一讲引入了 qubit 的概念,这一讲正式形式化:**叠加态的数学表示 → 多比特系统 → 纠缠 → 量子操作(门)**。

---

## 一、单 Qubit 的形式化

### 1. 纯态(Pure State)

- `|0⟩` 和 `|1⟩` 是两个**纯态**
- 物理对应:电子自旋上/下、光子的偏振方向等
- 注:本课程**不深入物理实现**,只关注算法层面

### 2. 叠加态(Superposition)的数学定义

一个量子态写成线性组合:

$$|\psi\rangle = \alpha_1 |0\rangle + \alpha_2 |1\rangle$$

- `|0⟩`、`|1⟩` 是向量,但不在普通向量空间,而在 **Hilbert 空间**(带内积的完备向量空间)
- 系数 `α₁`, `α₂` 是**复数**(可以是虚数!例:`i|0⟩ + |1⟩`)

### 3. 归一化条件

模长定义:

$$\||\psi\rangle\| = \sqrt{|\alpha_1|^2 + |\alpha_2|^2}$$

**所有量子态都必须归一化为模长 = 1**

示例:对 `|0⟩ + |1⟩`,模长 = √2,归一化后:

$$\frac{1}{\sqrt{2}}(|0\rangle + |1\rangle)$$

### 4. 测量规则(Born 规则)

测量后塌缩,概率 = 系数的模平方:

| 态 | 测得 `0` 概率 | 测得 `1` 概率 |
|---|---|---|
| `(1/√5)\|0⟩ + (2/√5)\|1⟩` | 1/5 | 4/5 |

### 5. 量子算法的基本流程

```
准备 qubits → 施加量子操作 → 测量 → 得到答案
```

---

## 二、振幅与相位

量子态既有 **振幅(amplitude)** 也有 **相位(phase)**。

- `|0⟩ + |1⟩` 和 `|0⟩ − |1⟩` 振幅相同(都是 1),**相位不同**
- 可在平面上把 qubit 视为有方向的**向量**(如各与 0 轴成 45°,但方向不同)
- → 算法将利用这种"方向性"

---

## 三、多 Qubit 系统

### 1. 组合规则:Kronecker 积(克罗内克积)

两个独立 qubit 合成时使用**张量积/Kronecker 积**:

- `c1 = |0⟩`, `c2 = |1⟩` → 合成 `|01⟩`
- 记号:`|01⟩` 表示比特 1 是 0、比特 2 是 1

### 2. 两 qubit 叠加示例

$$\frac{1}{2}(|00\rangle - |01\rangle + |10\rangle - |11\rangle)$$

测量结果:四种组合各以 1/4 概率出现。

### 3. 一般形式(n 维)

$$|\psi\rangle = \sum_{i=1}^{n} \alpha_i |s_i\rangle, \quad \sum_i |\alpha_i|^2 = 1$$

测得状态 `sᵢ` 的概率为 `|αᵢ|²`。

---

## 四、纠缠(Entanglement)⭐ 核心概念

### 1. 纠缠态的定义

考察这个两 qubit 态:

$$\frac{1}{\sqrt{2}}(|00\rangle + |11\rangle)$$

- 测量 q1 → 1/2 概率 0,1/2 概率 1
- **关键现象**:一旦测了 q1,q2 也同时塌缩!
  - q1 = 0 → q2 必然 = 0
  - q1 = 1 → q2 必然 = 1
- 测一个会让另一个跟着塌缩的状态 → **纠缠态**

### 2. 非纠缠态(可分态)

如果一个多 qubit 态能写成单 qubit 的 **Kronecker 积**,则不纠缠。

例:`(1/2)(|00⟩ − |01⟩ + |10⟩ − |11⟩) = b₁ ⊗ b₂`
→ 测 b1 不影响 b2,两者独立。

### 3. 数学判据

> **一个态是否纠缠 ⇔ 能否写成 Kronecker 积。**
> - 可分解 → 不纠缠
> - 不可分解 → 纠缠

### 4. 部分纠缠示例

$$\frac{1}{\sqrt{3}}(|00\rangle + |01\rangle + |11\rangle)$$

- 测 q2 = 0(概率 1/3)→ q1 完全塌缩到 0
- 测 q2 = 1(概率 2/3)→ q1 塌缩到叠加态 `(|0⟩+|1⟩)/√2`

→ 这是**部分纠缠**。

### 5. EPR 悖论(Einstein-Podolsky-Rosen)

- 一个原子静止,向相反方向发射两个光子
- 测量光子 A 的速度 → 光子 B 的位置同时塌缩
- 表面上看像"信息超光速传递",但实际上:
  - **两个光子是同一系统的两部分**,直到测量才"分裂"为两个独立实体
- 因此纠缠态又称 **EPR 态**
- **实验已证实纠缠真实存在**,是量子计算威力的来源

> 没有纠缠,量子算法做不了什么有意思的事;有了纠缠,才真正强大。

---

## 五、量子操作(Quantum Operations / Gates)

### 1. 三个核心性质

所有量子操作 T 必须满足:

| 性质 | 含义 |
|---|---|
| **线性(Linear)** | 只要知道 T 怎么作用在纯态上,就能算出对任意叠加态的作用 |
| **可逆(Reversible)** | 任何 q1 → q2 的演化都存在逆操作 q2 → q1(**测量是唯一例外**) |
| **酉性(Unitary)** | T\|0⟩ 与 T\|1⟩ 必须**正交**(内积为 0);保模长不变 |

### 2. 线性的具体含义

若已知:
- `T|0⟩ = β₁|0⟩ + β₂|1⟩`
- `T|1⟩ = γ₁|0⟩ + γ₂|1⟩`

则对任意叠加态:
$$T(\alpha_1|0\rangle + \alpha_2|1\rangle) = \alpha_1 T|0\rangle + \alpha_2 T|1\rangle$$

**矩阵表示**:

$$T = \begin{pmatrix} \beta_1 & \gamma_1 \\ \beta_2 & \gamma_2 \end{pmatrix}$$

---

## 六、常见单 Qubit 门

### 1. 量子非门(Quantum NOT / Pauli-X)

$$X = \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix}$$

作用:`|0⟩ ↔ |1⟩`,交换振幅。

### 2. Hadamard 门 ⭐(最重要!)

$$H = \frac{1}{\sqrt{2}}\begin{pmatrix} 1 & 1 \\ 1 & -1 \end{pmatrix}$$

作用规则:
- `H|0⟩ = (|0⟩ + |1⟩)/√2` (纯态 → 均匀叠加)
- `H|1⟩ = (|0⟩ − |1⟩)/√2`
- `H((|0⟩+|1⟩)/√2) = |0⟩` (叠加 → 纯态,可逆!)

**用途**:制造叠加态、量子并行性的核心工具。

### 3. 旋转门(Rotation by θ)

$$R(\theta) = \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}$$

将 qubit 视为平面向量,**逆时针旋转 θ**。

### 4. 反射门(Reflection)

将量子态绕某向量作几何反射。算法中(如 Grover 搜索)非常重要。

---

## 七、多 Qubit 操作

- 两 qubit 系统有 4 个基态:`|00⟩, |01⟩, |10⟩, |11⟩`
- 操作矩阵是 **4×4 酉矩阵**
- 状态空间维度 = `2ⁿ`(n 个 qubit)
- 无法在纸上画出(超 3 维),但数学完全适用

---

## 八、Bra-Ket 记号(简介)

- `|ψ⟩`(ket):**列向量**
- `⟨ψ|`(bra):**行向量**(ket 的共轭转置)
- `⟨ψ|φ⟩`:内积(bra-ket = "bracket",量子力学专用记号)

本课不深入,够用即可。

---

## 🎯 自测清单

1. 用什么数学条件区分**纠缠态**与**可分态**?
2. 一个 qubit 状态 `α|0⟩ + β|1⟩`,测得 0 的概率公式是什么?
3. 为什么所有量子门都必须是**酉矩阵**?(提示:模长守恒)
4. Hadamard 门作用在 `|0⟩` 上结果是什么?作用两次呢?
5. EPR 悖论中"超光速传递信息"是否真的发生?为什么?
6. 测量为什么是唯一**不可逆**的量子操作?
7. n 个 qubit 的状态空间维度是多少?这与经典 n 比特有何区别?
8. 为什么相位很重要,即使振幅相同?

---

## 🔮 下一讲预告

**Bell 不等式(Bell Inequality)** —— 用纠缠 qubit 能做到一些**经典比特无法做到**的事。
