# 贪心算法与Huffman编码 完整学习笔记

---

## 一、贪心算法概述

### 1.1 什么是贪心算法？

**贪心算法** = 在每一步决策时，根据某个**局部最优标准（贪心准则）**做出当前最好的选择，不考虑未来影响。

另一个名字：**近视算法（Myopic Algorithm）**——只看眼前，不做前瞻。

### 1.2 贪心 vs 动态规划

| | 动态规划 | 贪心算法 |
|--|---------|---------|
| 决策方式 | 考虑所有可能，前瞻未来 | 只看当前最优，不考虑未来 |
| 正确性 | 总是最优 | 不一定最优 |
| 速度 | 慢（建表，伪多项式） | 快（线性或接近线性） |
| 适用场景 | 需要精确最优解 | 快速近似，或恰好是最优 |

### 1.3 贪心算法适用的两种情况

1. **需要快速近似答案**，允许轻微非最优
2. **能证明贪心结果恰好是最优的**（最重要的情况）

---

## 二、贪心引例：钢条切割

**问题：** 80厘米的钢条，三种切法：

| 长度 | 价格 | 每厘米价格 |
|------|------|----------|
| 45cm | \$4.50 | 10分/cm |
| 40cm | \$3.90 | 9.75分/cm |
| 30cm | \$2.50 | 8.33分/cm |

**贪心准则：** 每次选每厘米价格最高的切法。

**贪心过程：**
```
剩余80cm → 选45cm（最高10分/cm）→ 获得$4.50，剩35cm
剩余35cm → 只能选30cm → 获得$2.50，剩5cm（浪费）
总收益：$7.00
```

**动态规划最优解：** 两段40cm，收益 **$7.80**

**结论：** 贪心在这里**不是最优的**。

---

## 三、贪心引例：硬币找零

**贪心准则：** 每次选不超过剩余金额的最大面值硬币。

### 标准美国面值（1, 5, 10, 25, 50分）

**找零78分：**
```
78 → 选50分 → 剩28分
28 → 选25分 → 剩3分
3  → 选3个1分
共5枚硬币（最优）✓
```

**结论：** 对标准美国面值，贪心算法**恰好是最优的**。

### 加入39分硬币后

- 贪心：50+25+3×1 = **5枚**
- 最优：39+39 = **2枚**

**结论：** 非标准面值时，贪心**不再最优**。

---

## 四、区间调度问题

### 4.1 问题定义

- 一个会议室，多个会议请求，每个会议有开始时间 $l_i$ 和结束时间 $u_i$
- **目标：** 安排尽可能多的不重叠会议

**现实应用：** 会议室预订、处理器任务调度、网络带宽分配

### 4.2 三种贪心策略比较

**策略1：优先选最短会议** ❌

反例：
```
M1: 0 ~ 6（6小时）
M2: 7 ~ 12（5小时）
M3: 5.5 ~ 7.5（2小时，最短）

选M3 → 只能安排1个
不选M3 → M1+M2 = 2个 ✓
```

**策略2：优先选最长会议** ❌

选了全天会议后，其他所有会议都被排除。

**策略3：优先选最早结束的会议** ✅ **最优！**

### 4.3 最早结束算法

```
1. 按结束时间排序所有会议
2. 选择结束时间最早的会议，安排它
3. 删除所有与已安排会议重叠的会议
4. 重复步骤2-3，直到没有会议剩余
```

### 4.4 手推例子

| 会议 | 时间 | 结束时间 |
|------|------|---------|
| M1 | 2 ~ 3.5 | 3.5 |
| M2 | 3.2 ~ 4.6 | 4.6 |
| M3 | 3.1 ~ 7.1 | 7.1 |
| M4 | 8 ~ 12 | 12 |
| M5 | 7 ~ 8.5 | 8.5 |
| M6 | 0 ~ 12 | 12 |

**执行过程：**
```
排序后：M1(3.5) < M2(4.6) < M3(7.1) < M5(8.5) < M4,M6(12)

选M1 → 删除重叠的M2、M3、M6
剩余：M4、M5
选M5 → 删除重叠的M4

最终：M1 + M5，共2个会议 ✓
```

### 4.5 最优性证明（交换论证）

**核心思路：**

假设"专家最优方案"安排了 $k$ 个会议，贪心方案可以逐步替换专家方案的每个选择：

```
专家选了会议X（结束时间 u_expert）
贪心选了会议G（结束时间 u_greedy ≤ u_expert）

→ 把X换成G，不产生新冲突（因为G结束更早）
→ 一换一，会议总数不变
→ 对每个会议重复此过程
→ 贪心方案 ≥ 专家最优方案
∴ 贪心就是最优！
```

### 4.6 最早结束策略的局限性

| 问题变种 | 是否适用 |
|---------|---------|
| **最大化会议数量（单房间）** | ✅ |
| 多个会议室 | ❌ |
| 最大化房间占用时间 | ❌ |
| 最大化收益（带价格） | ❌ |
| 优先级调度 | ❌ |

---

## 五、压缩与前缀码

### 5.1 为什么需要压缩？

ASCII用**8位**表示每个字符，无论频率高低：
- `E`（最常见）→ 8位
- `Z`（极少见）→ 8位

**解决思路：** 高频字符用**短编码**，低频字符用**长编码**，降低平均位数。

### 5.2 变长编码的问题

变长编码会产生**歧义**：

| 字符 | 编码 |
|------|------|
| A | 1 |
| C | 10 |
| T | 110 |

比特串 `110` 可以是 `A,C` 或 `T`，无法唯一解码！

### 5.3 前缀码（Prefix Code）

**定义：** 任何一个编码都**不是**另一个编码的前缀。

**合法前缀码示例：**

| 字符 | 编码 |
|------|------|
| A | `0` |
| C | `10` |
| T | `110` |
| G | `111` |

验证：`0` 不是 `10`、`110`、`111` 的前缀 ✓

**保证：** 任何比特串都有**唯一**的解码方式。

### 5.4 前缀码的树形表示

每个前缀码对应一棵**二叉树**：
- **叶子节点**：存放字符
- **左边分支**：0
- **右边分支**：1
- **字符的编码**：从根到该叶子的路径

```
        根
       / \
      A   ·
         / \
        C   ·
           / \
          T   G

A=0, C=10, T=110, G=111
```

**解码方法：** 从根出发，读一位走一步，到叶子就输出字符，回到根继续。

### 5.5 最优前缀码的目标

给定字符集和频率，找前缀树使**平均编码长度最小**：

```math
\text{树的代价} = \sum_{c \in \Sigma} f(c) \times d(c)
```

- $f(c)$：字符 $c$ 的频率
- $d(c)$：字符 $c$ 在树中的深度（= 编码位数）

---

## 六、Huffman编码

### 6.1 算法步骤（贪心）

```
1. 把所有字符放入集合，每个字符带频率

2. 取出频率最低的两个字符（或子树）

3. 合并成新子树：
   左分支(0) → 第一个字符
   右分支(1) → 第二个字符
   新节点频率 = 两者之和

4. 把新子树放回集合

5. 重复步骤2-4，直到只剩一棵树

6. 从根到每个叶子的路径即为该字符的编码
```

**贪心准则：** 每次合并**频率最低**的两个节点。

### 6.2 手推例子

**输入：** A=40%, T=40%, C=10%, G=10%

**第1步：** C(10) + G(10) → [CG](20)

```
[CG] 20%
/    \
C     G
```

**第2步：** [CG](20) + T(40) → [T,CG](60)

```
[T,CG] 60%
/       \
T       [CG]
        /   \
       C     G
```

**第3步：** A(40) + [T,CG](60) → 根(100)

```
        根(100)
       /        \
      A(40)    [T,CG](60)
               /        \
             T(40)      [CG](20)
                        /     \
                      C(10)  G(10)
```

**最终编码：**

| 字符 | 编码 | 位数 |
|------|------|------|
| A | `0` | 1 |
| T | `10` | 2 |
| C | `110` | 3 |
| G | `111` | 3 |

**平均位数：**

```math
0.4 \times 1 + 0.4 \times 2 + 0.1 \times 3 + 0.1 \times 3 = 1.8 \text{ 位/字符}
```

对比均匀编码（2位/字符），节省**10%**空间。

### 6.3 算法复杂度

使用**最小堆（优先队列）**实现：

```math
T(n) = O(n \log n)
```

每次取最小两个 $O(\log n)$，共 $n-1$ 次合并。

### 6.4 Huffman编码的重要性质

| 性质 | 说明 |
|------|------|
| 频率最低的字符位数最多 | 第一步就被合并，推到树最深处 |
| 频率最低的两个字符互为兄弟 | 第一步合并使它们深度相同 |
| 第二低频率字符位数也最多 | 与最低频率字符是兄弟，深度相同 |
| 最高频率字符不一定是1位 | 取决于字符数量和频率分布 |
| Huffman是最优前缀码 | 平均位数不可能更低 |

### 6.5 最优性证明（交换论证 + 数学归纳）

**两个关键性质（对任意最优树 $T^*$）：**

**性质1：** 最低频率的两个字符在树中**深度最大**。

证明：若有更深的字符 $a_k$，将 $a_i$（最低频率）与 $a_k$ 交换：

```math
\Delta = (f_i - f_k)(d_k - d_i) \leq 0
```

代价不增加，与 $T^*$ 最优矛盾，所以 $a_i, a_j$ 必须最深。

**性质2：** 最低频率的两个字符**互为兄弟**。

它们深度相同，若不是兄弟，交换其中一个与另一个的兄弟，代价不变，可使它们成为兄弟。

**归纳证明：**

```
基础情形：n=1,2 时唯一的树就是Huffman树，最优。

归纳步骤（n+1 → n）：
  给定n+1个字符的最优树T*
  → 由性质1,2，可把aᵢ,aⱼ调整为最深兄弟（代价不变）
  → 合并aᵢ,aⱼ为复合字符，得到n个字符的树T̂*
  → T̂*是n个字符的最优树（否则展开可改进T*，矛盾）
  → 由归纳假设，Huffman对n个字符产生最优树T̂
  → 展开T̂中的复合字符，得到Huffman对n+1个字符的树T
  → value(T) = value(T̂) + (fᵢ+fⱼ) = value(T̂*) + (fᵢ+fⱼ) = value(T*)
∴ Huffman树与最优树代价相同，Huffman最优！
```

---

## 七、贪心算法最优性的一般规律

两道题（区间调度、Huffman）证明方式都遵循同一模板：

```
1. 假设存在"专家最优方案"
        ↓
2. 证明可以把专家方案的第一步
   替换成贪心的第一步，而不使代价变差
        ↓
3. 剩余子问题规模缩小（归纳）
        ↓
4. 反复替换直到专家方案完全变成贪心方案
        ↓
5. 结论：贪心方案 ≥ 专家最优方案
   ∴ 贪心方案就是最优方案
```

背后的理论框架：**Matroid 理论（贪心拟阵理论）**，描述了什么条件下贪心算法一定能给出最优解。

---

## 八、重要贪心算法预告

以下是后续课程中**贪心且最优**的经典例子：

| 算法 | 问题 | 贪心准则 |
|------|------|---------|
| Dijkstra算法 | 单源最短路径 | 每次选距离最短的未访问节点 |
| Prim/Kruskal算法 | 最小生成树 | 每次选权重最小的边 |
| Huffman编码 | 最优前缀码 | 每次合并频率最低的两个节点 |
| 区间调度 | 最多不重叠区间 | 每次选最早结束的区间 |

---

## 九、常用速查

### 判断贪心是否最优的方法

1. **构造反例**：找一个贪心给出次优解的例子
2. **交换论证**：证明贪心选择可以替换最优解的选择而不变差
3. **数学归纳**：结合交换论证，用归纳法推广到所有规模

### Huffman编码步骤速查

```
输入：字符集和各字符频率
  ↓ 建最小堆
每次：取最小两个 → 合并 → 放回（重复n-1次）
  ↓
最终树即为Huffman树
  ↓
读编码：从根到叶子的路径（左=0，右=1）
  ↓
平均代价 = Σ f(c) × d(c)（最小值）
```
